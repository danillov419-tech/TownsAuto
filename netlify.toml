[build]
  command = "npm run build"
  publish = ".next"

# The official Next.js runtime for Netlify. Netlify auto-installs it, but we
# pin it here so builds are reproducible.
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
